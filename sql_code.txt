#Create Database
touch stock.db

#Connect to Database
sqlite3 stock.db

#Create table
CREATE TABLE history (
        date DATE,
        open NUMERIC,
        high NUMERIC,
        low NUMERIC,
        close NUMERIC,
        volume NUMERIC,
        symbol VARCHAR(10),
        growth NUMERIC);

Check available tables: sqlite>.tables

#Create table
CREATE TABLE live (
        date DATE,
        open NUMERIC,
        symbol VARCHAR(10));